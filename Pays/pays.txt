Exo 3 :SELECT * FROM t_regions INNER JOIN t_continents ON (t_continents.id_continent=t_regions.continent_id) WHERE libelle_continent LIKE 'Europe';SELECT * FROM t_pays INNER JOIN t_continents ON (t_continents.id_continent=t_pays.continent_id) WHERE libelle_continent LIKE 'Europe';SELECT * FROM t_pays INNER JOIN t_regions ON (t_regions.id_region=t_pays.region_id) WHERE libelle_region LIKE 'Afrique Centrale';SELECT count(*) FROM t_pays INNER JOIN t_continents ON (t_continents.id_continent=t_pays.continent_id) WHERE libelle_continent LIKE 'Océanie';SELECT count(*) FROM t_regions INNER JOIN t_continents ON (t_continents.id_continent=t_regions.continent_id) WHERE libelle_continent LIKE 'Asie';SELECT count(*) FROM t_regions INNER JOIN t_continents ON (t_continents.id_continent=t_regions.continent_id) WHERE libelle_continent LIKE 'Amérique Septentrionale';Exo 4 :SELECT SUM(population_pays), libelle_continent FROM t_pays INNER JOIN t_continents ON (t_continents.id_continent=t_pays.continent_id) GROUP BY libelle_continent;SELECT SUM(population_pays), libelle_region FROM t_pays INNER JOIN t_regions ON (t_regions.id_region=t_pays.region_id) GROUP BY libelle_region;SELECT libelle_continent,SUM(population_pays) AS population FROM t_pays INNER JOIN t_continents ON (t_continents.id_continent=t_pays.continent_id) GROUP BY id_continent HAVING population=(SELECT MAX(t0.somme) FROM (SELECT SUM(population_pays) AS somme FROM t_pays INNER JOIN t_continents ON (t_continents.id_continent=t_pays.continent_id) GROUP BY id_continent) as t0);SELECT libelle_region,SUM(population_pays) AS population FROM t_pays INNER JOIN t_regions ON (t_regions.id_region=t_pays.region_id) GROUP BY id_region HAVING population=(SELECT MAX(t0.somme) FROM (SELECT SUM(population_pays) AS somme FROM t_pays INNER JOIN t_regions ON (t_regions.id_region=t_pays.region_id) GROUP BY id_region) as t0);SELECT libelle_continent,SUM(population_pays) AS population FROM t_pays INNER JOIN t_continents ON (t_continents.id_continent=t_pays.continent_id) GROUP BY id_continent HAVING population=(SELECT MIN(t0.somme) FROM (SELECT SUM(population_pays) AS somme FROM t_pays INNER JOIN t_continents ON (t_continents.id_continent=t_pays.continent_id) GROUP BY id_continent) as t0);SELECT libelle_region,SUM(population_pays) AS population FROM t_pays INNER JOIN t_regions ON (t_regions.id_region=t_pays.region_id) GROUP BY id_region HAVING population=(SELECT MIN(t0.somme) FROM (SELECT SUM(population_pays) AS sommeFROM t_pays INNER JOIN t_regions ON (t_regions.id_region=t_pays.region_id) GROUP BY id_region) as t0);SELECT libelle_pays FROM t_pays ORDER BY esperance_vie_pays DESC LIMIT 1;SELECT libelle_pays FROM t_pays ORDER BY taux_mortalite_pays ASC LIMIT 1;SELECT libelle_pays FROM t_pays ORDER BY taux_natalite_pays DESC LIMIT 1; SELECT libelle_pays FROM t_pays ORDER BY nombre_enfants_par_femme_pays DESC LIMIT 1; SELECT libelle_pays FROM t_pays ORDER BY population_plus_65_pays DESC LIMIT 1; SELECT AVG(esperance_vie_pays) FROM t_paysSELECT AVG(taux_natalite_pays) FROM t_pays INNER JOIN t_regions ON (t_regions.id_region=t_pays.region_id) GROUP BY libelle_region;SELECT AVG(taux_natalite_pays) FROM t_pays INNER JOIN t_continents ON (t_continents.id_continent=t_pays.continent_id) GROUP BY libelle_continent;SELECT AVG(taux_mortalite_pays) FROM t_pays INNER JOIN t_regions ON (t_regions.id_region=t_pays.region_id) GROUP BY libelle_region;SELECT AVG(taux_mortalite_pays) FROM t_pays INNER JOIN t_continents ON (t_continents.id_continent=t_pays.continent_id) GROUP BY libelle_continent;